# 현재 폴더(clip/)에 있는 clip.py 모듈 안의 모든 클래스·함수·변수 등을 그대로 가져온다
# 이 한 줄만으로도 clip.clip이 아니라 clip 패키지 바로 아래에서 모델 로딩 함수나 토크나이저 등을 쓸 수 있도록 해 주는 역할을 한다
# 사용자 입장에서 import clip만으로 편리하게 쓰도록
# 내부 구현(어느 .py에 뭐가 들어있는지) 변경 시 외부 코드 영향 없이 유지·보수하도록

from .clip import *
